# Minimal PostgreSQL image configuration for WashApp
FROM postgres:15-alpine

# Default database credentials expected by the application
ENV POSTGRES_DB=washappDatabase \
    POSTGRES_USER=washapp \
    POSTGRES_PASSWORD=washapp123

EXPOSE 5432
